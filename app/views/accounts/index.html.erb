<div class="container">
  <div class="row">
    <div class="col-8">
        <% @posts.each do |post| %>
            <div><%= post.account.username %></div>
            <%= image_tag post.image %>
        <% end %>
    </div>
  </div>
</div>
